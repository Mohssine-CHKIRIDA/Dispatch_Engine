{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pulsaride.local/contracts/events/request.refused.schema.json",
  "title": "RequestRefused",
  "description": "Published by Dispatch Core when the proposed professional refuses or times out (FSM transition: proposed -> refused). Dispatch Core is expected to loop back and try the next candidate, which is why Evaluation tracks refusal rate per strategy as a key KPI.",
  "type": "object",
  "properties": {
    "event_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique ID for this specific event instance (not the request)."
    },
    "request_id": {
      "type": "string",
      "format": "uuid",
      "description": "Correlation ID shared by every event belonging to the same patient request, from submission to closure. This is how Evaluation reconstructs a full request lifecycle across services."
    },
    "event_type": {
      "const": "request.refused"
    },
    "version": {
      "type": "string",
      "description": "Schema version for this event type, e.g. '1.0'. Bump on breaking changes so consumers can branch on it during migration.",
      "default": "1.0"
    },
    "produced_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp of when the producing service emitted this event."
    },
    "producer": {
      "type": "string",
      "description": "Name of the service that published this event, e.g. 'ingestion', 'nlp-triage'. Useful for debugging and for Evaluation's traceability."
    },
    "payload": {
      "title": "RefusedPayload",
      "type": "object",
      "properties": {
        "pro_id": {
          "type": "string",
          "description": "ID of the professional who refused (or failed to respond in time)."
        },
        "reason": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "declined",
            "timeout",
            "unavailable",
            null
          ],
          "description": "Why the proposal didn't result in acceptance."
        },
        "retry_count": {
          "type": "integer",
          "minimum": 0,
          "description": "How many professionals have already been tried for this request_id, including this refusal."
        }
      },
      "required": [
        "pro_id",
        "retry_count"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "event_id",
    "request_id",
    "event_type",
    "version",
    "produced_at",
    "producer",
    "payload"
  ],
  "additionalProperties": false
}
