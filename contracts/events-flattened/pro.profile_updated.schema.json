{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pulsaride.local/contracts/events/pro.profile_updated.schema.json",
  "title": "ProProfileUpdated",
  "description": "Published by Pro Registry whenever a professional's profile changes (specialty, description, etc). IA3/matching_db keeps its embedding of the pro's profile in sync by re-embedding on this event.",
  "type": "object",
  "properties": {
    "event_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique ID for this specific event instance (not the request)."
    },
    "request_id": {
      "type": "string",
      "format": "uuid",
      "description": "Correlation ID shared by every event belonging to the same patient request, from submission to closure. This is how Evaluation reconstructs a full request lifecycle across services."
    },
    "event_type": {
      "const": "pro.profile_updated"
    },
    "version": {
      "type": "string",
      "description": "Schema version for this event type, e.g. '1.0'. Bump on breaking changes so consumers can branch on it during migration.",
      "default": "1.0"
    },
    "produced_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp of when the producing service emitted this event."
    },
    "producer": {
      "type": "string",
      "description": "Name of the service that published this event, e.g. 'ingestion', 'nlp-triage'. Useful for debugging and for Evaluation's traceability."
    },
    "payload": {
      "title": "ProfileUpdatedPayload",
      "type": "object",
      "properties": {
        "pro_id": {
          "type": "string",
          "description": "Professional's unique ID."
        },
        "specialty": {
          "type": "string",
          "description": "Declared specialty/profession, e.g. 'infirmier', 'médecin généraliste'."
        },
        "description": {
          "type": "string",
          "description": "Free-text profile description used as the basis for the semantic embedding (this is what gets compared against request.case_embedded via cosine similarity)."
        }
      },
      "required": [
        "pro_id",
        "specialty",
        "description"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "event_id",
    "request_id",
    "event_type",
    "version",
    "produced_at",
    "producer",
    "payload"
  ],
  "additionalProperties": false
}
